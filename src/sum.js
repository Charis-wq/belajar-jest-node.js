/*Membuat unit test
jest sudah menyadiakan  function yang di registasikan secara global bernama test(), function tersebut digunakan untuk membuat unit test
test() memiliki paramameter nama unit test dan juga function yang berisi kode unit testnya.*/

export const sum = (first, second) =>{
    return first + second ;
};

